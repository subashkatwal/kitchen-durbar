# Shell scripts run inside Linux containers (and are bind-mounted there in
# dev via docker-compose.override.yml) - they must keep LF endings even on a
# Windows checkout, or the kernel fails to exec them ("no such file").
*.sh text eol=lf
